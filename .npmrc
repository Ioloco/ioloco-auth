# Use symlinks for workspaces instead of copying node_modules
node-linker=pnpm

# Avoid duplicate lockfiles in workspace packages
shared-workspace-lockfile=true

# Don't auto-install peer deps — fail if they're missing
auto-install-peers=false

# Ensure peer dependencies must be explicitly installed
strict-peer-dependencies=true
